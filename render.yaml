services:
  - type: web
    name: qm-web-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: qm-db
          property: connectionString

databases:
  - name: qm-db
    plan: free
